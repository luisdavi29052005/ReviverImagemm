{
  "version": 2,
  "builds": [
    {
      "src": "server/stripe-server.js",
      "use": "@vercel/node"
    },
    {
      "src": "index.html",
      "use": "@vercel/static"
    }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/server/stripe-server.js" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
